// index.tsx
// Maps the default tab to the MealPlanScreen.
// This is the initial screen shown when the app loads.

import MealPlanScreen from '../screens/MealPlanScreen';

export default function IndexTab() {
  return <MealPlanScreen />;
}
